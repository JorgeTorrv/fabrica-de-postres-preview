# Solo para desarrollo local: apunta al backend corriendo en
# `wrangler pages dev` dentro de AdminPanel_FabricaPostres (puerto 8788).
# No afecta el build de producción (usa el default en src/lib/config.ts).
VITE_API_URL=http://localhost:8788
